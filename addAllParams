function addAllParams(...args){
    console.log(args);
    let sum = 0;
    args.forEach((e,i)=>{
        if(typeof e === 'number'){
            sum += e;
        }
        
    })
    return sum;
}
console.log(addAllParams(2,3,'i',4));
